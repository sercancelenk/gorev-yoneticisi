<%= form_for(@task) do |f| %>
  <% if @task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% @task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :gorev %><br>
    <%= f.text_field :gorev %>
  </div>
  <div class="field">
    <%= f.label :aciklama %><br>
    <%= f.text_area :aciklama %>
  </div>
  <div class="field">
    <%= f.label :tamamlandi %><br>
    <%= f.check_box :tamamlandi %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
